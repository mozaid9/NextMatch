class NotificationService {
  Future<void> initialise() async {
    // TODO(FCM): Request notification permissions and register device tokens.
  }

  Future<void> subscribeToMatch(String matchId) async {
    // TODO(FCM): Subscribe players to match updates and reminders.
  }
}
